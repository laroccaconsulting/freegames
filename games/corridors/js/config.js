// Online play needs the small server in worker/ (see worker/README.md).
// Leave this empty and "Play online" is hidden: the game stays fully static
// and offline. Set it to the server's URL, e.g.
//   'https://corridors.example.workers.dev'
// or to '/' when the server also hosts the game (wrangler dev / deploy).
export const ONLINE_SERVER = '';
